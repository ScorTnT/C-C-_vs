MAIN	SEGMENT
	ASSUME	CS:MAIN, DS:MAIN
	MOV AL, 12h
	MOV AX, 34
	MOV	DATA1, 34h
	MOV	BX, 5555h
	MOV	DX, 11A4h
	MOV	BX, DX
	MOV	BX, DX
	MOV	DH, BH
	MOV	DX, BX
	MOV	SUM, DX
	MOV	DL, 12h
	MOV	DL, 1h
	MOV 	SUM, 12h
	MOV 	SUM, 1h
	MOV 	SUM, 123h
	MOV 	SUM, 1234h
	MOV	DL, 255
	MOV 	DX, 5678h
SUM	DW	1234H
DATA1	DB	12H
MAIN	ENDS
	END